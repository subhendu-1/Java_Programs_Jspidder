class A 
{
	int a = 10;
	public static void main(String[] args) 
	{

		//We can't use else without if block
		//We can't use without if block
		int age = 38;
		
		/*if (age >= 24);  //This statement is not consider or farther lines of code execution // Here we terminate this function so this condition is  false or ture this not depend on after 
		{
			System.out.println("Gater than 18");
		} */
		//else {      // Here get error because without if condition not use else block
			//System.out.println("Less then 18");
		//}

		if (28 > 28.5)
		{
			//a == b;
		}
			if(age <40)
				System.out.println("Inner");
			System.out.println("Life");   // if we not using second bracat '{' 'if' condtion is ture then if block is terminated in this ';' 
		//else
			System.out.println("End");
        


		//if(age >28)
				//A d1 = new A();
				
		
//We can't use else without if block
		/*else{

		System.out.println("Any");
		}  */
	}
}
 